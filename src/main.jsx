import React from "react";
import ReactDOM from "react-dom/client";
import RecompLedger from "./RecompLedger.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RecompLedger />
  </React.StrictMode>
);
